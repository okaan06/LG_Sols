#define _CRT_sECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main(void) {


	int b = 5, c = 10, e = 9;
	double a = 6.9, d = 5.3, res;

	res = fabs(d - c) / (a + (b + pow(a, 3) / (1 + pow(c, 1.0 / 5)) / (e + sqrt(b + c)) / (c + pow(e, 2) / 4));

	printf("Result is: %0.4f", res);

























	return(0);
}